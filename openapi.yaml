openapi: 3.1.0
info:
  title: raphael-search
  version: 0.1.0
  description: Global and scoped search, semantic search
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
